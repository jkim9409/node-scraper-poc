const scrapingsController = require('./scrapingsController');
const simpleScrapingsController = require('./simpleScrapingsController');
// Import other controllers as needed

module.exports = {
    scrapingsController,
    simpleScrapingsController
    // Export other controllers here
};